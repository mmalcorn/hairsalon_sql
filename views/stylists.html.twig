<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
        <link href="/css/styles.css" rel="stylesheet" type="text/css">
        <title>Hair Salon</title>
    </head>
    <body>
      <div class="container">
        <h1>Blown Away Hair Salon</h1>
        <hr>
        <div class="col-xs-12">
          <h2>Stylists:</h2>
          {% if stylists is not empty %}
              <p>Here is a list of all of our stylists:</p>
              {% for stylist in stylists %}
                <p><h4><a href="/stylists/{{stylist.getId}}">{{stylist.getName}}</h4></a>
                  <a href="/stylists/{{stylist.getId}}/edit">Edit {{stylist.getName}}</a>
                </p>
              {% endfor %}
              <form action="/delete_stylists" method="post">
                <button class="btn btn-danger" type="submit">Delete All</button>
              </form>
          {% endif %}
        </div>
        <div class="col-xs-6">
          <h2>Add a new stylist:</h2>

          <form action="/add_stylists" method="post">
            <label for="add_stylist">Stylist name</label>
            <input id="add_stylist" type="text" name="add_stylist">
            <button type="submit" class="btn btn-success">Add</button>
          </form>
        </div>

        <div class="col-xs-12">
          <a href="/">Go Back</a>
        <div>
      </div>

    </body>
</html>
